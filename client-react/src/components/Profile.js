
import React from 'react';


const Profile = (props) => {

    // const { name, picture, email } = user;

    return(
        <div>
            {props.name}
        </div>
    )
    
};

export default Profile;